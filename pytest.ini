[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
addopts = -v --tb=short
markers =
    integration: tests requiring ML models (may be slow)
    e2e: end-to-end dashboard tests
    regression: edge case regression tests
